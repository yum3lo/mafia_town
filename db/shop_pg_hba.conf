# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# Allow all normal connections (lab-safe)
host    all             all             0.0.0.0/0               md5

# Allow replication connections
host    replication     replicator      0.0.0.0/0               md5
